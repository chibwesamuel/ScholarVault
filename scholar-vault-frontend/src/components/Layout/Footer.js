import React from 'react';

const Footer = () => {
    return (
        <footer className="footer">
            <p>&copy; {new Date().getFullYear()} ScholarVault</p>
            {/* Add footer content like links, company information, etc. */}
        </footer>
    );
};

export default Footer;